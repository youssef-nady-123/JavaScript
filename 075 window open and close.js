/*
    ===============================
    == 075 window open and close ==
    ===============================
    - open(URL, window name)
    - close()
    - window features
        width [num]
        height [num]
        left [num]
        top [num]
*/

// open sepcific page 
window.open('https://www.google.com/');

// close only the windows that were opened by the JS
window.close();
